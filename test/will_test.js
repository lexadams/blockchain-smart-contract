const Will = atrifacts.require("Will");

contract("Will", function (accounts) {
  it("should pass when both passwords are correct and deadline is not reached", function() {

  });

  it("should fail when one or both pieces of password are incorrect", function() {

  });
});
